[build]
  # Install and build explicitly inside the `client` folder so Netlify
  # runs `npm ci` in the frontend and publishes the built files from
  # `client/dist` regardless of the detected base directory.
  command = "npm ci --prefix client && npm run build --prefix client"
  publish = "client/dist"

[context.production.environment]
  NODE_ENV = "production"
